import { User } from '../../orm/entities/User';
/**
 * EntityTypes of all entities as string(lowercase) version
 */
export type EntityType = 'user';

/**
 * EntityClasses
 */
export type EntityClasses = User;
